<?xml version="1.0"?>
<ruleset name="LIMEDOCS">
    <description>Limedocs coding standard based on PSR2 with some additions.</description>

    <!-- Include most of the PSR2 standard -->
    <rule ref="PSR2">
        <exclude name="PSR2.Files.EndFileNewline.NoneFound"/>
        <exclude name="PSR2.Classes.ClassDeclaration.CloseBraceAfterBody"/>
    </rule>

    <!-- Exclude list -->
    <exclude-pattern>*/src/bootstrap.php</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/doc/*</exclude-pattern>
    <exclude-pattern>*/data/*</exclude-pattern>
    <exclude-pattern>*/bin/*</exclude-pattern>
    <exclude-pattern>*/config/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/utils/*</exclude-pattern>

</ruleset>
